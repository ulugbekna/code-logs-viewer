# Change Log

## 0.0.3

- Fold/unfold now toggles reliably on subsequent clicks (listener moved to row level; clicks inside the body no longer collapse the entry).
- Reverted: only entries with continuation lines are expandable.

## 0.0.2

- Every log entry is now expandable; rows without a body show a details panel (time, level, source, full message).
- Hover affordance on rows.

## 0.0.1

- Initial release.
